COMMON_FLAGS="-march=native -O3 -flto=thin -pipe -fomit-frame-pointer"
CFLAGS="${COMMON_FLAGS}"
CXXFLAGS="${COMMON_FLAGS} -stdlib=libc++"
FFLAGS="${COMMON_FLAGS}"
FCFLAGS="${COMMON_FLAGS}"
LDFLAGS="-Wl,-O3 -fuse-ld=lld -rtlib=compiler-rt -unwinlib=libunwind"
